// import { BarGraphData } from "@/components/Interface/interface";

// const fetchBarGraphData = async () => {
//     try {
//       const response = await fetch("http://localhost:5000/monthlyReport");

//       if (!response.ok) throw new Error("Failed to fetch chart data");
//       const apiData: BarGraphData[] = await response.json();

//       console.log(apiData);
//       setBarData(apiData);
//     } catch (error) {
//       setError(error instanceof Error ? error.message : "Unknown error");
//     }
//   };
